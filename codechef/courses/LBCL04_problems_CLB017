Multiplication
Write a program which prints these on two separate lines.

Result of multiplication of 8 and 9
Result of (100 + 200) * 2

#include <stdio.h>

int main() {
	printf("%d\n", _ * _);
	printf("____", (100 + 200) * _);
}

